#membuat kode function untuk menghitung faktorial dari nilai N

def faktorial(n):
    if n == 0 or n == 1:
        return 1
    else:
        return n * faktorial(n - 1)
    
# Minta input dari pengguna
angka = int(input("masukkan bilangan bulat non-negatif: "))

# Pastikan input valid (non-negatif)
if angka < 0:
    print("maaf, input harus bilangan bulat non-negatif.")
else:
    hasil = faktorial(angka)
    print(f"Faktorial dari {angka} adalah {hasil}")